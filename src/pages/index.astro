---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';
import SiteHeader from '../components/SiteHeader.astro';

const chapters = (await getCollection('chapters'))
  .filter((c) => !c.data.draft)
  .sort((a, b) => a.data.order - b.data.order);

// Parts are derived, not typed: a chapter's part is a function of its order,
// so inserting a chapter never means re-labelling the ones after it.
const PARTS = [
  { from: 1,  to: 7,  label: 'Il percorso della richiesta',  blurb: 'Come i byte su un socket diventano una richiesta da gestire.' },
  { from: 8,  to: 14, label: 'Stato e meccanismi',  blurb: 'Dove risiedono i dati e come interagiscono i componenti.' },
  { from: 15, to: 20, label: 'In produzione', blurb: 'Monitoraggio, sicurezza e prestazioni sotto carico.' },
  { from: 21, to: 24, label: 'Distribuzione e scalabilità', blurb: 'Distribuire, testare e collegare i servizi.' },
  { from: 25, to: 26, label: 'OpenAPI e agenti IA', blurb: 'Soluzioni con agenti IA pronte per la produzione' },
];
const grouped = PARTS.map((p) => ({
  ...p,
  chapters: chapters.filter((c) => c.data.order >= p.from && c.data.order <= p.to),
})).filter((p) => p.chapters.length > 0);
---
<Base
  title={`Backend dai principi fondamentali — una guida tecnica in ${chapters.length} capitoli`}
  description={`Guida allo sviluppo backend in ${chapters.length} capitoli: HTTP, routing, database, cache, Kafka e distribuzione in produzione. Appunti, diagrammi ed esempi eseguibili in Go e Python.`}
  path="/"
>
  <SiteHeader />

  <main class="home" id="main">
    <section class="hero">
      <p class="eyebrow">Una guida tecnica in {chapters.length} capitoli</p>
      <h1 class="hero-title">Backend, dai <em>principi fondamentali</em>.</h1>
      <p class="hero-lede">
        Molte risorse sul backend insegnano un framework. Questa guida spiega i meccanismi che lo fanno funzionare:
        che cosa sia una richiesta HTTP, come dimensionare un pool di connessioni e quali garanzie
        offra un broker. Ogni capitolo è una guida autonoma
        con diagrammi ed esempi eseguibili in Go e Python.
      </p>
      <div class="hero-actions">
        <a
          class="btn-continue"
          data-continue-reading
          hidden
          href="#"
        >
          <span data-continue-label>Continua a leggere</span>
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="2.5" y1="8" x2="12.5" y2="8" /><polyline points="8.5,4 12.5,8 8.5,12" /></svg>
        </a>
        <a
          class="btn-primary"
          data-start-reading
          href={chapters.length ? `/${chapters[0].id}/` : '#chapters'}
        >
          Inizia dal capitolo 01
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="2.5" y1="8" x2="12.5" y2="8" /><polyline points="8.5,4 12.5,8 8.5,12" /></svg>
        </a>
        <button class="btn-ghost" type="button" data-search-open>Cerca nella guida</button>
      </div>
      <p class="continue-meta" data-continue-meta hidden></p>

      <dl class="facts">
        <div class="fact"><dt>Capitoli</dt><dd>{chapters.length}</dd></div>
        <div class="fact"><dt>Linguaggi</dt><dd>Go · Python</dd></div>
        <div class="fact"><dt>Formato</dt><dd>Teoria + codice</dd></div>
        <div class="fact"><dt>Avanzamento</dt><dd data-progress-fact>0 / {chapters.length}</dd></div>
      </dl>
    </section>

    <section class="parts" id="chapters" aria-label="Tutti i capitoli">
      {grouped.map((part) => (
        <section class="part">
          <header class="part-head">
            <h2 class="part-title">{part.label}</h2>
            <p class="part-blurb">{part.blurb}</p>
            <p class="part-range">
              {String(part.from).padStart(2, '0')}–{String(part.to).padStart(2, '0')}
            </p>
            <p class="part-progress" data-part-progress>0 / {part.chapters.length}</p>
          </header>

          <ul class="grid">
            {part.chapters.map((c) => (
              <li>
                <a class="card" href={`/${c.id}/`} data-chapter-id={c.id}>
                  <span class="card-num">{String(c.data.order).padStart(2, '0')}</span>
                  <span class="card-body">
                    <span class="card-title">{c.data.navTitle ?? c.data.title}</span>
                    <span class="card-summary">{c.data.summary}</span>
                  </span>
                  <span class="card-foot">
                    <span class="card-time">{c.data.readingTime}</span>
                    <span class="card-check">
                      <span aria-hidden="true">✓</span><span class="sr-only">Completato</span>
                    </span>
                  </span>
                </a>
              </li>
            ))}
          </ul>
        </section>
      ))}
    </section>

    <section class="contribute">
      <div class="contribute-inner">
        <p class="eyebrow">Open source</p>
        <h2 class="contribute-title">Questi appunti migliorano grazie al confronto.</h2>
        <p class="contribute-body">
          Hai trovato una spiegazione poco chiara, un caso limite mancante o un diagramma errato?
          I capitoli sono in Markdown: puoi proporre una correzione con una pull request.
          Sono particolarmente gradite le implementazioni
          in altri linguaggi.
        </p>
        <div class="contribute-actions">
          <a class="btn-primary" href="https://github.com/DsThakurRawat/Backend-from-first-Principle" target="_blank" rel="noopener noreferrer">Contribuisci su GitHub</a>
          <a class="btn-ghost" href="https://github.com/DsThakurRawat/Backend-from-first-Principle/issues" target="_blank" rel="noopener noreferrer">Segnala un problema</a>
        </div>
      </div>
    </section>

    <footer class="site-footer">
      <p>A cura di <a href="https://github.com/DsThakurRawat" target="_blank" rel="noopener noreferrer">@DsThakurRawat</a></p>
      <p class="quote">“Impara i fondamenti e i framework diventeranno chiari.”</p>
    </footer>
  </main>
</Base>

<style>
  .home {
    max-width: 74rem;
    margin-inline: auto;
    padding-inline: clamp(var(--space-4), 4vw, var(--space-7));
  }

  .eyebrow {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-caps);
    text-transform: uppercase;
    color: var(--accent);
  }

  /* --- hero ------------------------------------------------------------- */
  .hero { padding-block: clamp(var(--space-8), 11vh, var(--space-10)) var(--space-9); }

  .hero-title {
    margin-top: var(--space-5);
    font-size: var(--text-4xl);
    font-weight: 660;
    letter-spacing: -0.038em;
    max-width: 16ch;
  }
  .hero-title em {
    font-family: var(--font-prose);
    font-style: italic;
    font-weight: 400;
    letter-spacing: -0.02em;
    color: var(--accent);
  }

  .hero-lede {
    margin-top: var(--space-5);
    max-width: 60ch;
    font-family: var(--font-prose);
    font-size: var(--text-xl);
    line-height: 1.6;
    color: var(--ink-2);
    text-wrap: pretty;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: var(--space-6);
    align-items: center;
  }

  .btn-primary, .btn-ghost, .btn-continue {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    height: 42px;
    padding-inline: var(--space-5);
    border-radius: var(--radius);
    font-size: var(--text-sm);
    font-weight: 600;
    text-decoration: none;
    white-space: nowrap;
    transition: background-color 120ms ease, border-color 120ms ease, color 120ms ease, transform 120ms ease;
  }
  .btn-primary { background: var(--accent); color: var(--accent-ink); border: 1px solid var(--accent); }
  .btn-primary:hover { transform: translateY(-1px); }
  .btn-primary svg,
  .btn-continue svg { width: 15px; height: 15px; flex: none; }
  .btn-primary[hidden], .btn-ghost[hidden], .btn-continue[hidden] { display: none; }
  /* When Continue leads, Start steps down to an accent outline — still
     distinct from Search (ghost). */
  .btn-primary.is-secondary {
    background: transparent;
    color: var(--accent);
    border-color: color-mix(in oklab, var(--accent) 35%, var(--line));
  }
  .btn-primary.is-secondary:hover {
    background: var(--accent-bg);
    border-color: var(--accent);
    transform: none;
  }
  .btn-ghost { background: var(--surface); color: var(--ink); border: 1px solid var(--line); }
  .btn-ghost:hover { border-color: var(--line-2); }

  .btn-continue {
    background: var(--accent);
    color: var(--accent-ink);
    border: 1px solid var(--accent);
  }
  .btn-continue:hover { transform: translateY(-1px); }

  .continue-meta {
    margin: var(--space-2) 0 0;
    max-width: 40rem;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-wide);
    color: var(--ink-3);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .continue-meta[hidden] { display: none; }

  @media (max-width: 560px) {
    .hero-actions { flex-direction: column; align-items: stretch; }
    .btn-continue,
    .btn-primary[data-start-reading],
    .btn-ghost[data-search-open] {
      width: 100%;
    }
    .continue-meta { white-space: normal; }
  }

  .facts {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-7);
    margin-top: var(--space-8);
    padding-top: var(--space-5);
    border-top: 1px solid var(--line);
  }
  .fact dt {
    font-size: var(--text-2xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: var(--tracking-caps);
    color: var(--ink-3);
  }
  .fact dd {
    margin: var(--space-1) 0 0;
    font-family: var(--font-mono);
    font-size: var(--text-lg);
    font-weight: 500;
  }

  /* --- parts + chapter grid --------------------------------------------- */
  .part + .part { margin-top: var(--space-9); }

  .part-head {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: baseline;
    gap: var(--space-3) var(--space-5);
    padding-bottom: var(--space-4);
    border-bottom: 1px solid var(--line);
    margin-bottom: var(--space-5);
  }
  .part-title { font-size: var(--text-xl); font-weight: 640; }
  .part-range {
    grid-row: 1;
    grid-column: 2;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--ink-3);
    letter-spacing: var(--tracking-wide);
  }
  .part-blurb {
    grid-column: 1 / -1;
    font-size: var(--text-sm);
    color: var(--ink-3);
  }
  .part-progress {
    grid-row: 3;
    grid-column: 2;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--ink-3);
    letter-spacing: var(--tracking-wide);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(19rem, 1fr));
    gap: var(--space-3);
    list-style: none;
    padding: 0;
  }

  .card {
    display: grid;
    grid-template-columns: auto 1fr;
    grid-template-rows: auto auto;
    column-gap: var(--space-4);
    row-gap: var(--space-2);
    height: 100%;
    padding: var(--space-4) var(--space-5);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: var(--surface);
    text-decoration: none;
    transition: border-color 140ms ease, background-color 140ms ease, transform 140ms ease;
  }
  .card:hover {
    border-color: var(--accent);
    background: var(--accent-bg);
    transform: translateY(-2px);
  }
  .card.is-done { border-color: var(--accent); }

  .card-check {
    display: none;
    color: var(--accent);
    font-size: var(--text-sm);
    font-weight: 700;
    line-height: 1;
  }
  .card.is-done .card-check { display: block; }

  .card-num {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--accent);
    padding-top: 0.15em;
  }
  .card-body { display: flex; flex-direction: column; gap: var(--space-2); min-width: 0; }
  .card-title { font-weight: 620; font-size: var(--text-base); line-height: 1.3; text-wrap: balance; }
  .card-summary {
    font-family: var(--font-prose);
    font-size: var(--text-sm);
    line-height: 1.5;
    color: var(--ink-2);
    display: -webkit-box;
    -webkit-line-clamp: 3;
    line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .card-foot {
    grid-column: 2;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
  }
  .card-time {
    font-family: var(--font-mono);
    font-size: var(--text-2xs);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--ink-3);
  }

  /* --- contribute ------------------------------------------------------- */
  .contribute { margin-top: var(--space-10); }
  .contribute-inner {
    padding: clamp(var(--space-6), 5vw, var(--space-8));
    border: 1px solid var(--line);
    border-radius: var(--radius-lg);
    background: var(--surface);
  }
  .contribute-title {
    margin-top: var(--space-4);
    font-size: var(--text-2xl);
    max-width: 22ch;
  }
  .contribute-body {
    margin-top: var(--space-4);
    max-width: 62ch;
    font-family: var(--font-prose);
    font-size: var(--text-lg);
    color: var(--ink-2);
    text-wrap: pretty;
  }
  .contribute-actions { display: flex; flex-wrap: wrap; gap: var(--space-3); margin-top: var(--space-5); }

  /* --- footer ----------------------------------------------------------- */
  .site-footer {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: var(--space-4);
    margin-top: var(--space-9);
    padding-block: var(--space-6) var(--space-8);
    border-top: 1px solid var(--line);
    font-size: var(--text-sm);
    color: var(--ink-3);
  }
  .site-footer a { color: var(--ink-2); }
  .quote { font-family: var(--font-prose); font-style: italic; }

  @media (max-width: 560px) {
    .part-head { grid-template-columns: 1fr; }
    .part-range { grid-row: auto; grid-column: 1; }
    .part-progress { grid-row: auto; grid-column: 1; }
  }
</style>

<script>
  const PROGRESS_KEY = 'bfp-progress';
  const POSITION_KEY = 'bfp-reading-position';

  const readProgress = (): Set<string> => {
    try {
      const raw = JSON.parse(localStorage.getItem(PROGRESS_KEY) ?? '[]');
      return new Set<string>(Array.isArray(raw) ? raw : []);
    } catch { return new Set(); }
  };

  type PositionEntry = {
    sectionId?: string;
    sectionTitle?: string;
    chapterTitle?: string;
    chapterNum?: string;
    scrollY?: number;
    updatedAt?: number;
  };

  const readPositions = (): { lastId?: string; positions: Record<string, PositionEntry> } => {
    try {
      const raw = JSON.parse(localStorage.getItem(POSITION_KEY) ?? '{}');
      if (!raw || typeof raw !== 'object' || Array.isArray(raw)) {
        return { positions: {} };
      }
      const positions =
        raw.positions && typeof raw.positions === 'object' && !Array.isArray(raw.positions)
          ? (raw.positions as Record<string, PositionEntry>)
          : {};
      return {
        lastId: typeof raw.lastId === 'string' ? raw.lastId : undefined,
        positions,
      };
    } catch {
      return { positions: {} };
    }
  };

  const continueLink = document.querySelector<HTMLAnchorElement>('[data-continue-reading]');
  const continueLabel = document.querySelector<HTMLElement>('[data-continue-label]');
  const continueMeta = document.querySelector<HTMLElement>('[data-continue-meta]');
  const startLink = document.querySelector<HTMLAnchorElement>('[data-start-reading]');

  const renderContinue = () => {
    if (!continueLink || !continueLabel || !startLink) return;

    const store = readPositions();
    const id = store.lastId;
    const entry = id ? store.positions[id] : undefined;
    const known = id
      ? Boolean(document.querySelector(`[data-chapter-id="${CSS.escape(id)}"]`))
      : false;

    if (!id || !entry || !known || (!(entry.scrollY && entry.scrollY > 180) && !entry.sectionId)) {
      continueLink.hidden = true;
      continueLink.removeAttribute('href');
      continueLink.removeAttribute('aria-label');
      continueLink.removeAttribute('title');
      continueLabel.textContent = 'Continua a leggere';
      if (continueMeta) {
        continueMeta.hidden = true;
        continueMeta.textContent = '';
      }
      startLink.classList.remove('is-secondary');
      return;
    }

    const hash = entry.sectionId ? `#${entry.sectionId}` : '';
    continueLink.href = `/${id}/${hash}`;

    const num = entry.chapterNum ? `Cap. ${entry.chapterNum}` : '';
    const section = entry.sectionTitle || entry.chapterTitle || '';
    const meta = [num, section].filter(Boolean).join(' → ');
    const fullParts = [
      entry.chapterNum && entry.chapterTitle
        ? `Capitolo ${entry.chapterNum}: ${entry.chapterTitle}`
        : (entry.chapterTitle || num || 'your chapter'),
      entry.sectionTitle,
    ].filter(Boolean);
    const full = `Continua a leggere → ${fullParts.join(' → ')}`;

    continueLabel.textContent = 'Continua a leggere';
    continueLink.title = full;
    continueLink.setAttribute('aria-label', full);
    if (continueMeta) {
      continueMeta.textContent = meta || 'Riprendi da dove eri rimasto';
      continueMeta.hidden = false;
    }
    continueLink.hidden = false;
    startLink.classList.add('is-secondary');
  };

  // Re-run on pageshow (bfcache restores the DOM without re-executing this
  // script, so a completion made on another page and then reached via the
  // back button would otherwise render stale) and on storage (another tab
  // changing the same key).
  const render = () => {
    const done = readProgress();

    // Every count is derived from the cards actually rendered, never from
    // the size of the stored list: a renamed or retired chapter leaves a
    // stale id behind, and a stale id must not show up as 25 / 24.
    let doneCount = 0;
    let cardCount = 0;

    document.querySelectorAll<HTMLElement>('.part').forEach((part) => {
      const cards = part.querySelectorAll<HTMLAnchorElement>('[data-chapter-id]');
      let completed = 0;

      cards.forEach((card) => {
        const isDone = done.has(card.dataset.chapterId!);
        card.classList.toggle('is-done', isDone);
        if (isDone) completed += 1;
      });

      const label = part.querySelector<HTMLElement>('[data-part-progress]');
      if (label) label.textContent = `${completed} / ${cards.length}`;

      doneCount += completed;
      cardCount += cards.length;
    });

    const progressFact = document.querySelector<HTMLElement>('[data-progress-fact]');
    if (progressFact) progressFact.textContent = `${doneCount} / ${cardCount}`;

    renderContinue();
  };

  render();
  addEventListener('pageshow', render);
  addEventListener('storage', (e) => {
    // e.key is null when another tab calls localStorage.clear().
    if (e.key === PROGRESS_KEY || e.key === POSITION_KEY || e.key === null) render();
  });
</script>
